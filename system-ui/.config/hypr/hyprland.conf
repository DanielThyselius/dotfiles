# Learn how to configure Hyprland: https://wiki.hyprland.org/Configuring/

# Use defaults Omarchy defaults (but don't edit these directly!)
source = ~/.local/share/omarchy/default/hypr/autostart.conf
source = ~/.local/share/omarchy/default/hypr/bindings/media.conf
source = ~/.local/share/omarchy/default/hypr/bindings/clipboard.conf
source = ~/.local/share/omarchy/default/hypr/bindings/tiling-v2.conf
source = ~/.local/share/omarchy/default/hypr/bindings/utilities.conf
source = ~/.local/share/omarchy/default/hypr/envs.conf
source = ~/.local/share/omarchy/default/hypr/looknfeel.conf
source = ~/.local/share/omarchy/default/hypr/input.conf
source = ~/.local/share/omarchy/default/hypr/windows.conf
source = ~/.local/state/omarchy/current/theme/hyprland.conf

# Change your own setup in these files (and overwrite any settings from defaults!)
source = ~/.config/hypr/monitors.conf
source = ~/.config/hypr/input.conf
source = ~/.config/hypr/bindings.conf
source = ~/.config/hypr/looknfeel.conf
source = ~/.config/hypr/autostart.conf

# Add any other personal Hyprland configuration below
# windowrule = workspace 5, match:class qemu

# Workspace overview (Mission-Control style) — DISABLED 2026-06-01.
# hyprexpo was dropped upstream as unmaintained (2026-05-12) and its successor
# hyprtasking does not yet compile against Hyprland 0.55.2 (API breakage).
# Re-enable when hyprtasking ports to 0.55.x (or omarchy ships a native overview):
#   1. hyprpm add https://github.com/raybbian/hyprtasking && hyprpm enable hyprtasking
#   2. uncomment this block + the SUPER+R bind (bindings.conf) + gestures (input.conf)
# Requires the `meson` + `ninja` packages to build (already installed).
# plugin {
#     hyprtasking {
#         layout = grid
#         gap_size = 5
#         bg_color = 0xff111111
#         grid {
#             rows = 3
#             cols = 3
#         }
#     }
# }

# Toggle config flags dynamically
source = ~/.local/state/omarchy/toggles/hypr/*.conf
